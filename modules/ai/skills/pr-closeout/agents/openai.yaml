interface:
  display_name: "PR Closeout"
  short_description: "Finish branches with reviewable commits"
  default_prompt: "Use $pr-closeout to finish this branch with clean commits, autoreview loops, validation gates, and PR status matching the user and repository instructions."
